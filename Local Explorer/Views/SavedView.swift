
import SwiftUI

struct SavedView: View {
    var body: some View {
        VStack {
            Text("SavedView Page")
                .font(.largeTitle)
            Spacer()
        }
        .padding()
    }
}
